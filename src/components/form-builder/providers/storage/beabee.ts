const beabee = (formio) => {
  return {
    title: 'beabee',
    name: 'beabee',
    uploadFile(
      file,
      name,
      dir,
      progressCallback,
      url,
      options,
      fileKey,
      groupPermissions,
      groupId,
      abortCallback
    ) {},
  };
};

beabee.title = 'beabee';
export default beabee;
